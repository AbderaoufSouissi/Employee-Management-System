spring:
  application:
    name: ems-backend
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/ems-db?createDatabaseIfNotExist=true
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
server:
  port: 8088

